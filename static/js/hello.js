window.onload = function() {
    setTimeout(function() {
        alert("Hello, world!");
    }, 5000); // The timeout value is just a number, not "timeout:5000"
}; // Don't forget the semicolon at the end of the window.onload assignment